name: govbot-x-weekly-digest

on:
  # FRIDAY, first slot. X is a text-first news/politics feed, so the week-in-
  # review lands best while the legislative week is still live news and the
  # weekday desk audience is online — Friday mid-morning, before the afternoon
  # drop-off. The visual feeds (Threads, Instagram) run Saturday instead.
  #
  # NOTE the lead time: cron fires the *workflow*, not the post. The prepare job
  # clones 50+ states first, so the first post lands roughly 45-70 minutes later.
  # 13:07 UTC therefore posts at ~14:00-14:20 UTC ≈ 10:00-10:20 AM ET. Adjust the
  # cron by that lead, not by the time you want to see the post.
  #
  # GitHub cron is fixed UTC and ignores DST, so during EST this lands an hour
  # earlier local. Odd minute dodges top-of-hour scheduler congestion.
  schedule:
    - cron: "7 13 * * FRI"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: govbot-x-weekly-digest
  cancel-in-progress: false

defaults:
  run:
    shell: bash

# The X feed is a single account (one set of X_* secrets), so the X digest
# runs for the one X-enabled topic rather than looping every topics/ folder
# the way the all-Bluesky-accounts digest does. Set BOT_TOPIC below if the X
# bot is ever pointed at a different topic.

jobs:
  # ---------------------------------------------------------------------------
  # Shared setup, on its own runner. The govbot clone across 50+ states is the
  # slow, disk-heavy half of this workflow. Doing it here, distilling it to a
  # compact bills.jsonl (+ staged full-text bundle) and shipping that as an
  # artifact means the digest job never holds the giant clone AND the Ollama
  # model at once — and, more to the point, each half gets a full time budget of
  # its own instead of the two sharing one. Mirrors post_to_x.yml.
  # ---------------------------------------------------------------------------
  prepare:
    runs-on: ubuntu-latest
    # The digests always clone fresh (shared-cache is off, see below), so this
    # job pays the full national clone every week. 120 min gives that ample room.
    timeout-minutes: 120

    env:
      # Import bootstrap only: stage_bill_text.py needs a valid topic to import
      # the shared modules, but it stages across every topics/ folder.
      BOT_TOPIC: "ai_data_centers"

    steps:
      - name: Free disk space
        # ubuntu-latest ships with ~14 GB free; govbot clones across 50+ states
        # blow past that, which makes the runner crash with "No space left on
        # device". Strip out preinstalled tooling we never use first.
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install Python deps
        run: pip install -r requirements.txt

      - name: Install govbot, fetch all supported states, dump JSONL
        # Resilient install (retries the daily-republished upstream nightly).
        # shared-cache is OFF: all four weekly digests run Friday with the same
        # inputs, so with it on only the first would clone and the rest would
        # reuse that dump and SKIP the clone — leaving no on-disk metadata or
        # files/ dir for the staging step below to harvest. Cloning fresh keeps
        # govbot's committed full text on disk so digest summaries stay grounded.
        uses: ./.github/actions/govbot-bills
        with:
          shared-cache: "false"
          # Full-action feed — same as the daily posters. Dump EVERY action, not
          # just govbot's noteworthy ones, so a bill active only procedurally in
          # the lookback window still reaches the digest. since-days (45) sits
          # above the widest fallback window (30d) and, paired with --limit none,
          # keeps the larger feed bounded and free of arbitrary per-state
          # truncation. The digest's own topic match + 7/14/30d windows still
          # rank highlights.
          log-filter: "none"
          log-limit: "none"
          since-days: "45"

      - name: Stage the full-text bundle
        # govbot-bills only stages this itself when shared-cache is on, and the
        # digests keep it off — so do it explicitly here. Copies each in-window
        # bill's metadata.json + govbot's pre-extracted text out of the clone
        # into one small tarball, which is what lets the digest job render
        # grounded summaries without re-cloning. stage_bill_text.py already
        # stages wide enough (45 days) to cover the digests' 30-day lookback.
        # Non-fatal: an empty bundle just means summaries fall back to abstracts.
        run: |
          if python scripts/stage_bill_text.py shared_bill_text; then
            echo "staged full text for $(find shared_bill_text -name metadata.json | wc -l) bill(s)."
          else
            echo "::warning::full-text staging failed; shipping an empty bundle (abstracts only)."
            mkdir -p shared_bill_text
          fi
          tar -czf shared_bill_text.tar.gz shared_bill_text

      - name: Upload the shared bill corpus
        uses: actions/upload-artifact@v4
        with:
          name: bills-corpus
          path: |
            bills.jsonl
            shared_bill_text.tar.gz
          if-no-files-found: error
          retention-days: 1
          compression-level: 9

  # ---------------------------------------------------------------------------
  # Build and post. Downloads the corpus (no re-clone) and only installs Ollama
  # locally, so this runner holds the compact corpus + the model, never the
  # full clone.
  # ---------------------------------------------------------------------------
  digest:
    needs: prepare
    runs-on: ubuntu-latest
    # No govbot clone here (the corpus is downloaded), so this is Ollama setup
    # plus the digest's own LLM work — which on the free runner's CPU model is
    # slow enough to have been squeezed by the old shared 60. 120 min gives the
    # summaries room to finish.
    timeout-minutes: 120

    env:
      LLM_MODEL: "gemma3:4b"
      BOT_TOPIC: "ai_data_centers"
      DRY_RUN: "0"
      DIGEST_LOOKBACK_DAYS: "7"
      DIGEST_MAX_HIGHLIGHTS: "6"
      DIGEST_PER_STATE_CAP: "2"

    steps:
      - name: Free disk space
        # ubuntu-latest ships with ~14 GB free; govbot clones across 50+ states
        # plus the Ollama LLM blow past that, which makes the runner crash with
        # "No space left on device". Strip out preinstalled tooling we never
        # use to recover ~30 GB before anything else runs.
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false  # keep swap: the LLM + full bill corpus need the RAM headroom

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install poppler-utils (pdftotext)
        # Lets scripts/bill_text.py extract full bill text from PDFs for the
        # digest summaries; degrades gracefully to abstract-only if absent.
        run: sudo apt-get update && sudo apt-get install -y poppler-utils

      - name: Install Python deps
        run: pip install -r requirements.txt

      - name: Download the shared bill corpus
        # Lands bills.jsonl (+ shared_bill_text.tar.gz) in the repo root, exactly
        # where the scripts expect them — no govbot clone needed in this job.
        uses: actions/download-artifact@v4
        with:
          name: bills-corpus
          path: .

      - name: Confirm corpus present and unpack staged bill text
        # The posting step below sets GOVBOT_DATA_ROOT to this dir so
        # bill_text.resolve_metadata_path finds the staged subset exactly as it
        # would a full clone. A missing/empty tarball is non-fatal: the digest
        # just falls back to abstracts.
        run: |
          test -f bills.jsonl && echo "bills.jsonl: $(wc -l < bills.jsonl) lines" \
            || (echo "::error::bills.jsonl missing after download" && exit 1)
          if [ -f shared_bill_text.tar.gz ]; then
            tar -xzf shared_bill_text.tar.gz
            n=$(find shared_bill_text -name metadata.json 2>/dev/null | wc -l)
            echo "staged bill text: $n bill(s) with metadata unpacked."
          else
            echo "no staged bill text tarball; digest will use abstracts only."
          fi

      - name: Cache Ollama model
        uses: actions/cache@v4
        with:
          path: ~/.ollama/models
          key: ollama-models-${{ env.LLM_MODEL }}-v1

      - name: Cache Ollama binary
        # Bump the -v1 suffix to force a fresh install (e.g. to pick up a
        # new Ollama release).
        uses: actions/cache@v4
        with:
          path: ~/.ollama-bin
          key: ollama-bin-v1-linux-amd64

      - name: Cache extracted bill text
        # bill_text.py caches PDF-extracted text in .bill_text_cache/, keyed by
        # a hash of the document URL — content-addressed, so entries never go
        # stale. Persisting it across runs skips re-downloading and re-running
        # pdftotext for bills already processed on an earlier day (bills stay in
        # the freshness window for weeks). The run-scoped key always saves a
        # fresh, grown cache; restore-keys pulls the most recent prior one, and
        # the shared prefix lets every poster reuse text any other already
        # fetched.
        uses: actions/cache@v4
        with:
          path: .bill_text_cache
          key: bill-text-cache-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            bill-text-cache-

      - name: Install Ollama and pull the LLM model
        run: |
          if [ -f "$HOME/.ollama-bin/usr-local.tar" ]; then
            echo "Restoring Ollama binary from cache."
            sudo tar -C / -xf "$HOME/.ollama-bin/usr-local.tar"
          else
            curl -fsSL https://ollama.com/install.sh | sh
            mkdir -p "$HOME/.ollama-bin"
            PATHS="usr/local/bin/ollama"
            [ -d /usr/local/lib/ollama ] && PATHS="$PATHS usr/local/lib/ollama"
            sudo tar -C / -cf "$HOME/.ollama-bin/usr-local.tar" $PATHS
            sudo chown -R "$USER:$USER" "$HOME/.ollama-bin"
          fi
          nohup ollama serve > /tmp/ollama.log 2>&1 &
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:11434/api/tags > /dev/null 2>&1; then
              echo "Ollama is ready."
              break
            fi
            sleep 1
          done
          if ollama list | awk 'NR>1 {print $1}' | grep -qx "$LLM_MODEL"; then
            echo "$LLM_MODEL already present, skipping pull."
          else
            ollama pull "$LLM_MODEL"
          fi
          ollama list
          # Smoke-test the model, but DON'T pin it in memory (keep_alive:0
          # unloads it right after). Holding the 3.3GB model resident while the
          # next step loads the full bill corpus into RAM pushed the 16GB runner
          # over its memory limit, freezing the VM until GitHub killed the job
          # mid-step ("runner has received a shutdown signal"). The first real
          # summary reloads the model on demand.
          curl -fsS http://localhost:11434/api/generate \
            -d "{\"model\":\"$LLM_MODEL\",\"prompt\":\"ok\",\"stream\":false,\"keep_alive\":\"0\"}" \
            > /dev/null 2>&1 || echo "  (warm-up failed; model will load on first summary)"

      - name: Build and post X weekly digest thread
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          LLM_API_URL: "http://localhost:11434/api/chat"
          # Point bill_text.resolve_metadata_path at the staged subset the
          # prepare job shipped, so full bill text is used even though this job
          # never cloned.
          GOVBOT_DATA_ROOT: "${{ github.workspace }}/shared_bill_text"
        run: python scripts/weekly_digest_x.py

      - name: Commit X digest artifacts
        if: env.DRY_RUN != '1'
        run: |
          git config user.name  "govbot-x[bot]"
          git config user.email "govbot-x[bot]@users.noreply.github.com"
          git add topics/*/x*/weekly_digest/bills_raw topics/*/x*/weekly_digest/bills_full_text || true
          if git diff --cached --quiet; then
            echo "No X digest artifacts to commit."
          else
            git commit -m "chore: update X weekly-digest artifacts [skip ci]"
            git push
          fi
